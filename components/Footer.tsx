export default function Footer() {
  return (
    <footer className="mt-16 border-t border-ink-line/60">
      <div className="mx-auto flex max-w-5xl flex-col gap-1 px-6 py-8 text-xs text-paper-dim">
        <p>Tolong Scannin Di! — alat internal tim, bukan buat publik ya.</p>
        <p>Semua proses jalan lokal di browser kamu. Gak ada dokumen yang keupload ke server mana pun — aman!</p>
        <p className="mt-1 text-paper-dim/70">Dibikin sama Adi 🙌 — nemu bug atau ada request fitur, langsung samperin aja.</p>
      </div>
    </footer>
  );
}
